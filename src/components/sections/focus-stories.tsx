import { EditorialImage } from '@/components/ui/editorial-image';import { TextLink } from '@/components/ui/text-link';import type { LinkItem } from '@/content/site-pages';
export function FocusStories({ items }: { items: LinkItem[] }){return <div className="focus-stories">{items.map((item,index)=><article className="focus-story" key={item.label}><EditorialImage src={item.image ?? '/images/practice-detail.jpg'} alt={`Bildplatz für ${item.label}`} aspect={index===1?'portrait':'landscape'}/><div><p className="eyebrow">Schwerpunkt</p><h3>{item.label}</h3><p>{item.description}</p><TextLink href={item.href}>Mehr zu {item.label}</TextLink></div></article>)}</div>}
