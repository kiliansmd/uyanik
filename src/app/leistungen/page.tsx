import Link from 'next/link';import { PageHero } from '@/components/sections/page-hero';import { servicePages } from '@/content/services';
export default function ServicesPage(){return <><PageHero title="HNO-Leistungen" eyebrow="Übersicht"><p>Die Leistungsbereiche werden fachlich geprüft und datengetrieben gepflegt, damit Inhalte konsistent bleiben.</p></PageHero><section className="section"><div className="container grid gap-5 md:grid-cols-2">{servicePages.map((service)=><Link className="card p-6" href={`/leistungen/${service.slug}`} key={service.slug}><h2 className="text-2xl font-semibold">{service.title}</h2><p className="mt-3 text-[var(--color-muted)]">{service.summary}</p></Link>)}</div></section></>}
