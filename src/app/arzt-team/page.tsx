import type { Metadata } from 'next';
import { Container } from '@/components/layout/container';import { Section } from '@/components/layout/section';import { PageHero } from '@/components/sections/page-hero';import { ProcessSteps } from '@/components/sections/process-steps';import { EditorialImage } from '@/components/ui/editorial-image';import { teamMembers } from '@/content/team';import { seoPages } from '@/content/site-pages';import { createPageMetadata } from '@/lib/metadata';
export const metadata: Metadata = createPageMetadata(seoPages.team);
export default function TeamPage(){return <><PageHero title="Dr. med. Bedros Uyanik und das Praxisteam" eyebrow="Arzt und Team"><p>Die Praxis steht für eine persönliche Haltung, verständliche Kommunikation und sorgfältige Diagnostik. Erwachsene und Kinder werden im konservativen und operativen HNO-Bereich begleitet.</p></PageHero><Section><Container className="split-section"><EditorialImage src="/images/doctor-portrait.jpg" alt="Bildplatz für ein freizugebendes Porträt von Dr. Uyanik" aspect="portrait"/><div className="service-index">{teamMembers.map((member,index)=><article className="service-row" key={member.name}><span>{String(index+1).padStart(2,'0')}</span><div><h2>{member.name}</h2><p>{member.role}</p>{member.note ? <small>{member.note}</small> : null}</div></article>)}</div></Container></Section><Section tone="paper"><Container><ProcessSteps steps={['Fachärztliche HNO-Beratung mit persönlicher und verständlicher Einordnung.','Sorgfältige Diagnostik für Erwachsene und Kinder je nach Beschwerdebild.','Konservative und operative Behandlungsmöglichkeiten werden nach Befund besprochen.']}/></Container></Section></>}
