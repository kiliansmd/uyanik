import { PageHero } from '@/components/sections/page-hero';import { teamMembers } from '@/content/team';
export default function TeamPage(){return <><PageHero title="Arzt & Team" eyebrow="Persönlich"><p>Die Praxis stellt persönliche Betreuung und klare Kommunikation in den Mittelpunkt.</p></PageHero><section className="section"><div className="container grid gap-4">{teamMembers.map((member)=><article className="card p-6" key={member.name}><h2 className="text-2xl font-semibold">{member.name}</h2><p>{member.role}</p>{member.note ? <p className="mt-2 text-[var(--color-muted)]">{member.note}</p> : null}</article>)}</div></section></>}
